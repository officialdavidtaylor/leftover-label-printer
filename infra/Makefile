SHELL := /bin/sh
SERVICE := infra

.PHONY: install lint test build

install:
	@printf "[%s] install: placeholder for infrastructure bootstrap tasks (pending implementation).\n" "$(SERVICE)"

lint:
	@printf "[%s] lint: placeholder for IaC/schema lint checks (pending implementation).\n" "$(SERVICE)"

test:
	@printf "[%s] test: placeholder for infra validation tests (pending implementation).\n" "$(SERVICE)"

build:
	@printf "[%s] build: placeholder for infra packaging/generation steps (pending implementation).\n" "$(SERVICE)"
