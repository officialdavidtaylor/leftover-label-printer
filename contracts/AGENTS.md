# Contracts AGENTS

Scope: `contracts/` (interface source of truth).

- HTTP contract: `openapi.yaml`; MQTT contract: `asyncapi.yaml`.
- Follow semver for contract changes.
- Breaking changes require deprecation window + migration notes.
- Contract edits require matching contract test updates.
