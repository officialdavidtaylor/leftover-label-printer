SHELL := /bin/sh
SERVICE := agent

.PHONY: install lint test build

install:
	@printf "[%s] install: placeholder for Go module/tool setup (pending service implementation).\n" "$(SERVICE)"

lint:
	@printf "[%s] lint: placeholder for golangci-lint/static checks (pending service implementation).\n" "$(SERVICE)"

test:
	@printf "[%s] test: placeholder for Go unit tests (pending service implementation).\n" "$(SERVICE)"

build:
	@printf "[%s] build: placeholder for agent binary/container build (pending service implementation).\n" "$(SERVICE)"
